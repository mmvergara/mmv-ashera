export type noteDetail = { noteTitle: string; noteContent: string };
export interface noteNoId {
  noteContent: string;
  noteAuthor: string;
  noteTitle: string;
}
export interface noteDetails {
  noteContent: string;
  noteAuthor: string;
  noteTitle: string;
  noteId: string;
}
